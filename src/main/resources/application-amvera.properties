# Профиль amvera: постоянное хранилище монтируется в /data (persistenceMount)
server.address=0.0.0.0
server.port=8080
spring.datasource.url=jdbc:h2:file:/data/reportdb;DB_CLOSE_DELAY=-1
report.backup.directory=/data/backups
